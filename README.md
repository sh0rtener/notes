# ShNotes

**ShNotes** — простой REST API для работы с заметками, разработанный на **ASP.NET Core**.

Проект построен с разделением ответственности между слоями приложения и ориентирован на создание поддерживаемой и расширяемой backend-архитектуры.

## Возможности

* Создание заметок
* Просмотр списка заметок
* Поиск и фильтрация заметок
* Просмотр отдельной заметки
* Изменение названия и описания заметки
* Изменение статуса заметки
* Удаление заметок
* Хранение заметок в базе данных
* Система пользователей
* Credentials и хранение паролей в виде хэша
* Привязка заметок к пользователям
* Кеширование результатов запросов
* Инвалидация кеша при изменении данных
* Работа с заметками через REST API
* Интерактивная документация API через Swagger

## Стек технологий

* **.NET 8**
* **ASP.NET Core Web API**
* **Entity Framework Core**
* **SQLite**
* **MediatR**
* **AutoMapper**
* **Swagger / OpenAPI**
* **xUnit**
* **IMemoryCache**

## Архитектура

Проект разделён на несколько слоёв:

```text
src/
├── Core/
│   ├── ShNotes.Core/
│   └── ShNotes.UseCases/
│
├── Infra/
│   ├── ShNotes.Data/
│   └── ShNotes.Caching/
│
└── Presenters/
    └── ShNotes.WebApi/

tests/
└── ShNotes.Tests/
```

### Core

Содержит основные сущности и абстракции предметной области.

```text
ShNotes.Core
├── Entities
├── Value Objects
└── Domain exceptions
```

Слой не зависит от инфраструктуры или Web API.

### UseCases

Содержит прикладную логику и сценарии использования приложения.

```text
ShNotes.UseCases
├── Notes
│   ├── AddNote
│   ├── GetNotes
│   ├── GetNote
│   ├── ChangeNoteName
│   ├── ChangeNoteDescription
│   ├── ChangeNoteStatus
│   └── RemoveNote
│
└── Users
    ├── CreateUser
    ├── GetUser
    └── SignIn
```

Для организации use case'ов используется **MediatR**.

Такой подход позволяет отделить прикладную логику от HTTP-слоя и не перегружать контроллеры бизнес-логикой.

### Data

Слой отвечает за работу с базой данных и реализацию инфраструктурных зависимостей.

```text
ShNotes.Data
├── Daos
├── EntityFramework
├── SQL Scripts
└── Dependency Injection
```

Для доступа к данным используется **Entity Framework Core**.

Модели базы данных отделены от доменных сущностей с помощью DAO.

В качестве базы данных на данный момент используется **SQLite**.

### Caching

Слой отвечает за кэширование данных и инвалидацию связанных кешей.

```text
ShNotes.Caching
├── InMemory
├── CacheInvalidator
└── Dependency Injection
```

Для кэширования используется **IMemoryCache**.

Кэширование вынесено в отдельный слой и не смешивается с основной бизнес-логикой use case'ов.

### Web API

Слой представления приложения, предоставляющий HTTP API.

```text
ShNotes.WebApi
├── Controllers
├── Middlewares
├── Common
└── Program.cs
```

Контроллеры отвечают преимущественно за обработку HTTP-запросов и передачу управления соответствующим use case'ам.

Для документации и тестирования API используется **Swagger / OpenAPI**.

## Возможности

На текущий момент реализованы:

* получение списка заметок;
* фильтрация заметок;
* получение отдельной заметки;
* создание заметок;
* изменение названия и описания;
* изменение статуса;
* удаление заметок;
* DTO для передачи данных;
* разделение Domain и Data моделей;
* Repository abstraction;
* обработка use case'ов через MediatR;
* глобальная обработка ошибок;
* система пользователей;
* credentials;
* привязка заметок к пользователям;
* in-memory кеширование;
* инвалидация кеша;
* Swagger / OpenAPI;
* хранение данных в SQLite;
* отдельный проект для тестов.

## API

### Получение заметок

```http
GET /notes
```

Пример запроса с фильтрацией:

```http
GET /notes?name=example
```

Пример ответа:

```json
[
  {
    "id": 1,
    "name": "Example",
    "description": "Example note"
  }
]
```

### Получение заметки

```http
GET /notes/{id}
```

### Создание заметки

```http
POST /notes
```

### Изменение заметки

```http
PATCH /notes/{id}/name
PATCH /notes/{id}/description
```

### Изменение статуса

```http
PATCH /notes/{id}/status
```

### Удаление заметки

```http
DELETE /notes/{id}
```

Более подробное описание endpoints и моделей доступно через Swagger UI после запуска приложения.

## Запуск проекта

### Требования

Для запуска необходим:

* [.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)
* Git

### Клонирование репозитория

```bash
git clone https://github.com/sh0rtener/notes.git
cd notes
```

### Восстановление зависимостей

```bash
dotnet restore
```

### Сборка

```bash
dotnet build
```

### Запуск

```bash
dotnet run --project src/Presenters/ShNotes.WebApi
```

После запуска API будет доступен по адресу, указанному ASP.NET Core в консоли.

В режиме разработки доступен Swagger UI.

## Тестирование

Для запуска тестов:

```bash
dotnet test
```

Тесты находятся в:

```text
tests/ShNotes.Tests
```

## Цели проекта

Проект создаётся как практическая backend-разработка с упором на:

* разделение ответственности;
* изоляцию доменной логики;
* тестируемость;
* абстрагирование работы с базой данных;
* отделение инфраструктуры от бизнес-логики;
* поддерживаемость кода;
* возможность дальнейшего расширения приложения.

## Планируемые улучшения

* [x] Система пользователей
* [x] Credentials
* [x] Привязка заметок к пользователям
* [x] Кеширование
* [x] Создание, изменение и удаление заметок
* [ ] Работа без авторизации через сессию
* [ ] Расширение покрытия тестами
* [ ] Поддержка дополнительных СУБД
* [ ] Docker
* [ ] Production-конфигурация
